spring:
  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

logging:
  level:
    root: WARN
    org.springframework: WARN
    org.hibernate: WARN
    org.springframework.test: WARN
