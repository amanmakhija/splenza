spring:
  application:
    name: splitwise-backend

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/splitwise}
    username: ${SPRING_DATASOURCE_USERNAME:splitwise}
    password: ${SPRING_DATASOURCE_PASSWORD:splitwise_dev_password}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080
  error:
    include-message: always

jwt:
  secret: ${JWT_SECRET:dev_only_secret_key_please_change_me_32chars}
  expiration-ms: ${JWT_EXPIRATION_MS:900000} # 15 min access token
  refresh-expiration-ms: ${JWT_REFRESH_EXPIRATION_MS:604800000} # 7 days

aws:
  s3:
    bucket: ${AWS_S3_BUCKET:splitwise-receipts}
    region: ${AWS_REGION:ap-south-1}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

logging:
  level:
    com.splitwise.app: DEBUG
    org.springframework.security: INFO

google:
  oauth:
    # Create this in Google Cloud Console > APIs & Services > Credentials > OAuth Client ID
    # (type: Android, package name com.splenza.app, plus your release/debug SHA-1 fingerprints).
    # For the ID token audience check to work, use the WEB client ID here (Google issues one
    # automatically alongside the Android client) - see README for the full walkthrough.
    client-id: ${GOOGLE_OAUTH_CLIENT_ID:}
